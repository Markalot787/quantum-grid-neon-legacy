To fix the white screen issue with your Quantum Grid: Neon Legacy game, follow these steps:

1. The main error is: "Failed to resolve module specifier 'three'". This means your browser can't find the Three.js library.

2. Here's what to do:

   A. Update your index.html file:
      - Make sure your script tag includes the `type="module"` attribute:
      ```html
      <script type="module" src="main.js"></script>
      ```

   B. Fix all THREE.js imports in your code:
      - Open each JavaScript file that imports Three.js (Game.js, Cube.js, Player.js, Level.js)
      - Make sure the import uses the CDN URL:
      ```javascript
      import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.157.0/build/three.module.js';
      ```

   C. Remove the cube rolling animation:
      - Open public/game/Cube.js
      - Remove these lines in the constructor:
        ```javascript
        this.rollAngle = 0;
        this.lastPosition = new THREE.Vector3(x, this.size / 2, z);
        ```
      - In the update method, remove this code block:
        ```javascript
        // Add rolling animation
        const movement = new THREE.Vector3().subVectors(this.lastPosition, currentPosition);
        const distance = movement.length();
        
        if (distance > 0.01) {
          // Determine rotation axis (perpendicular to movement direction)
          const rotationAxis = new THREE.Vector3(movement.z, 0, -movement.x).normalize();
          
          // Calculate rotation amount
          const rotationAmount = distance / (this.size / 2) * Math.PI / 2;
          
          // Apply rotation
          this.mesh.rotateOnAxis(rotationAxis, rotationAmount);
        }
        
        // Store position for next frame
        this.lastPosition.copy(this.mesh.position);
        ```

   D. Remove the hovering/floating effect:
      - Also in Cube.js, remove these lines from the update method:
        ```javascript
        // Add floating/hovering effect
        const hoverOffset = Math.sin(this.game.clock.elapsedTime * 2 + this.animationPhase) * 0.05;
        this.mesh.position.y = this.size / 2 + hoverOffset;
        ```

3. Update your server.js file:
   ```javascript
   const express = require('express');
   const path = require('path');
   const app = express();
   const PORT = process.env.PORT || 3000;

   // Set proper MIME types for JavaScript modules
   app.use((req, res, next) => {
     if (req.path.endsWith('.js')) {
       res.set('Content-Type', 'application/javascript');
     }
     next();
   });

   // Serve static files
   app.use(express.static(path.join(__dirname, 'public')));

   app.get('/', (req, res) => {
     res.sendFile(path.join(__dirname, 'public', 'index.html'));
   });

   app.listen(PORT, () => {
     console.log(`Quantum Grid: Neon Legacy running on port ${PORT}`);
   });
   ```

4. Restart your server and refresh your browser

These changes should fix the white screen issue and remove the two animations you mentioned. The game will still have the other visual improvements but without the rolling and hovering animations.

If you're still having issues, please check your browser console for specific error messages. 